// Package main implements the commitmsg command.
//
// The command is a pure-Go, standard-library-only reimplementation of the
// lightninglabs/darepo-client commit message convention. It is kept in the
// repository so commit message checks do not depend on Python, Node, or an
// external shell package manager.
package main
